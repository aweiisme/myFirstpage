$(document).ready(function () {

    //確認點擊gotop按鈕
    $('#gotop').click(function (e) {

        //停止一開始的連結動作
        e.preventDefault();

        //發現連結對象
        var target = $(e.currentTarget).attr("href");

        //確認連結對象位置
        var offset = $(target).offset();

        //執行scroll動畫往上滾動
        $('html, body').animate({ scrollTop: offset.top });

    })
    $('#welc').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#ab_me').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#skt').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#intrs').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#callme').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#mbg').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#pcg').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#olg').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })
    $('#og').click(function (e) {
        e.preventDefault();
        var target = $(e.currentTarget).attr("href");
        var offset = $(target).offset();
        $('html, body').animate({ scrollTop: offset.top });
    })

})



// bootstrap原本無法使用popover以及tooltip兩個東西
// 所一這邊要自己添加js來啟用
// $(document).ready(function () {
//     $('[data-toggle="popover"]').popover({
//         placement: 'top'
//     });
// });

// $(document).ready(function () {
//     $('[data-toggle="tooltip"]').tooltip({});
// });

// $(document).ready(function () {
//     $(document).scroll(function (e) {
//         console.log($(e.currentTarget).scrollTop());
//     });
// });

// $(document).ready(function () {
//     $(document).scroll(function (e) {
//         if ($(this).scrollTop() > 2250) {
//             $('.fade').addClass('in');
//         };
//     })
// })

